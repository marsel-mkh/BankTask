spring:
  application:
    name: client-processing
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.r2dbc.R2dbcAutoConfiguration

  datasource:
    url: jdbc:postgresql://localhost:5432/t1db
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  liquibase:
    enabled: true
    default-schema: public
    change-log: classpath:db/changelog/db.changelog-master.xml

  kafka:
    bootstrap-servers: localhost:9092
    properties:
      spring.json.trusted.packages: "*"

  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    org.springframework.security: trace


server:
  port: 8082

kafka:
  topic:
    client-products: "client_products"
    client-cards: "client_cards"
    credit-products: "client_credit_products"
    service-log: "service_logs"

security:
  token: tstrs4g434wyhser5htstrs4g434wyhser5htstrs4g434wyhser5htstrs4g434wyhser5htstrs4g434wyhser5h
  expiration: 86400000